# bash config should live in ~/.bashrc. source it here so it’s always loaded.
# http://en.wikipedia.org/wiki/Bash_(Unix_shell)#Startup_scripts
# https://github.com/ryanb/dotfiles/blob/master/bash_profile

if [ -f ~/.bashrc ]; then
  source ~/.bashrc
fi
